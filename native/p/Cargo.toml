[package]
name = "p"
version = "0.1.0"
authors = []
edition = "2021"

[lib]
name = "p"
crate-type = ["cdylib"]

[dependencies]
lazy_static = "1.5.0"
nix = { version = "0.29.0", features = ["signal", "process"] }
rustler = "0.36.1"
